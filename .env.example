# PostgreSQL connection
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/zogency_dev"
# Auth.js session encryption secret — generate with: openssl rand -base64 32
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"
# Integration credential encryption key (AES-256-GCM) — generate with: openssl rand -base64 32
# Falls back to AUTH_SECRET if unset. Rotating it requires reconnecting integrations.
ZOGENCY_CREDENTIALS_KEY=""
# Redis (required from Sprint 2 for BullMQ workers)
# REDIS_URL="redis://localhost:6379"
